# 保存目录实施清单

## 依赖与门禁

- [x] 记录用户要求开始执行, 明确允许使用 Android Studio 模拟器.
- [x] 核对任务指针与真实依赖: 本任务是第二子任务, 初次核对时前置任务仍为 planning, 随后已先完成其独立验收.
- [x] 保留 main 分支已有 55 项未提交改动, 不提交或归档.
- [x] 启动 Pixel_10 并验证 ADB、PNG、screenrecord、拉取大小一致和 MP4 解码, 见 [preflight.md](./preflight.md).
- [x] 2026-09-05 用户审阅后明确授权由主线程分配并执行, 按先文件/应用/生码偏好、再保存目录与失败保留的顺序推进.
- [x] 文件应用与生码子任务完成实现和独立检查, 见前置 validation.md; 在其最终 schema/dialog 上继续, 不并行修改.
- [x] 读取 trellis-before-dev, frontend 设置/桥接/质量规范, backend 录屏/文件下载/错误规范, 父任务 research.
- [x] 确认 main 未提交改动, 开始本任务; 记录原偏好, 不删除第一批用户文件.

## 1. 保存目录唯一合约

- [x] 新增 capture.directory 偏好, 保留旧配置兼容和单目录/全组两种重置范围.
- [x] 统一两份 screenshot_dir, default/custom 显式类型, 移除硬编码 /tmp 回落.
- [x] 原生目录选择/取消/验证/异步代次/写入失败, 复用 Tauri bridge 和现有 capability.
- [x] 测试 null 默认、自定义路径、相对路径拒绝、目录不存在/不是目录、Pictures 缺失、取消与保存失败.
- [x] Browser 检查目录长路径/双尺寸/焦点, 原生选择确认不自动附加子目录或改变开关.

## 2. 先完成录屏失败保留

- [x] 明确 session_id 和 recording/pending_save/saving/save_failed, 全局只允许一个会话, IO 不持全程锁.
- [x] 重排保存为停止 -> 源大小 -> 暂存 pull -> 完整校验 -> 发布 -> 删源 -> 打开.
- [x] 失败保留会话/源, 清理只限本次部分文件, 防重复保存/放弃和迟到会话操作.
- [x] 成功保存但手机清理失败/外部打开失败分别反馈, 不丢掉已保存路径.
- [x] Rust 定向测试每步失败、零/不完整文件、并发保存、新 start 拒绝、旧 ID 拒绝、成功后才删源.

## 3. 接入真实工具流程

- [x] 截图点击快照和录屏开始目录快照传必填参数, 更新所有 TS/Rust 调用与注册.
- [x] 截图并复制绕过目录, 旧结果打开/定位使用真实返回路径.
- [x] 统一手停/自然结束/切设备 finalize, 每会话自动尝试一次, 失败后只允许手动恢复.
- [x] 加入重试/原生另存为/取消/确认放弃, 对话框返回仍校验会话 ID.
- [x] 测试设置读取失败、pending 状态多次轮询、StrictMode、迟到对话框和复合错误不触发重复保存.
- [x] 更新录屏提示和错误操作区, 不增加新的工作区或恢复管理页.

## 4. Android 模拟器与本机 IO 验收

- [x] LaunchServices 启动 debug .app, 连接 Pixel_10, 选择本机中文/空格测试目录, 保存并检查 PNG/MP4 有效. 本轮设备验收按用户要求使用模拟器, 真机兼容性单独注明未覆盖.
- [x] 录制中从目录 A 改 B, 当前段留 A, 下一段去 B, 保存打开开关仍按停止时值执行.
- [x] 已验证关闭仍保存、目录与全组重置范围、重试时开启打开并实际启动 QuickTime. 截图自动打开/定位全部组合未重复, 见 validation.md.
- [x] 截图并复制在自定义目录不可用时仍能写图片剪贴板.
- [x] 专用目录移动故障验证源和会话保留、没有自动重试; 未注入系统 ACL 或真实设备断线.
- [x] 使用另存为恢复有效 MP4, 验证成功后才清理手机源; 不改变全局目录.
- [x] 取消另存为/取消放弃无副作用, 原生确认放弃释放会话并删除对应源; 残留错误由单测和代码审查覆盖.
- [x] 自然结束与设备切换触发路径一致, 无第二设备时用控制器覆盖迟到操作并注明设备实测上限.
- [x] 原生重启后目录保留, 单目录/整组重置正确, 不移动/删除旧文件.

## 5. 完整检查与记录

~~~sh
corepack pnpm test
corepack pnpm build
perl -e 'alarm shift; exec @ARGV' 60 cargo test --manifest-path src-tauri/Cargo.toml
cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
git diff --check
~~~

- [x] 更新旧录屏 spec 与新合约, 两份 README 同步目录说明.
- [x] trellis-check 检查失败时序、第二状态源、宽泛吞错、残留 /tmp fallback、漏参数、隐式覆盖和测试真实性.
- [x] 向父任务记录平台/设备/实测产物/未覆盖范围, 恢复测试偏好后执行联合验收.
- [x] 用户要求后再提交, 不自动归档.

## 回滚

先处理尚未保存会话, 不直接退出或替换应用. 按 UI/协议/状态/目录 helper 一起撤回, 不删用户偏好或手机源文件. 已知旧版会重新忽略/丢弃新目录字段, 交付中记录此兼容上限.

2026-09-05 已完成实现与主要工作流验收, 详见 validation.md. 代码保持未提交, 不运行会自动提交的归档脚本.
