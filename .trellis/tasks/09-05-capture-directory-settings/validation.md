# 保存目录与录屏恢复验收

## 交付状态

- 2026-09-05 用户批准由主线程安排并执行, 已按前置浏览/生码子任务到本任务的顺序完成实现.
- 公共保存目录、原生选择与重置、唯一输出服务、录屏会话 ID、失败保留、重试/另存为/确认放弃均已接入.
- 同步 TypeScript/Rust 必填合约, 移除截图/录屏的两份目录解析及 /tmp 回落. 日志和报告目录保持原有规则.
- 使用 tempfile 的完整暂存与不覆盖发布, 显式另存为允许系统确认的替换. 自动打开或源清理失败独立反馈, 不抹去已保存文件路径.
- 只承诺应用进程内恢复, 未添加持久化恢复队列. 测试结束无待保存会话, 未提交或归档.

## 自动检查

| 检查 | 结果 |
| --- | --- |
| 最终前端测试 | 37 个文件, 380 项通过 |
| 恢复控制器/目录设置/轮询定向测试 | 3 个文件, 29 项通过 |
| Rust 全量测试, 60 秒上限 | 125 项通过 |
| 计时冻结修正后的 Rust 录屏定向测试 | 3 项通过 |
| TypeScript/Vite 与 Tauri debug app | 构建通过, 保留既有大 chunk 警告 |
| cargo fmt --check / clippy -D warnings | 通过 |
| git diff --check | 通过 |

测试涵盖 Pictures 缺失、无效/被移走的自定义目录、目标冲突不覆盖、零文件/部分拉取、发布失败、成功后才调用清理、旧 ID 和独占会话、设置失败后的自动尝试抑制、重复停止、对话框取消/迟到、dispose/resume 与清理错误独立反馈.

## 原生实际操作

环境为 LaunchServices 启动的 macOS debug .app, Pixel_10, emulator-5554, Android 17 / SDK 37. 所有故障只针对本轮 /tmp/adb-gui-capture-test.vmHpQY 下的专用目录和应用新建的设备源文件.

1. 原生设置显示实际默认 /Users/qi/Pictures/ADB GUI. 选择中文/空格目录 A 后路径保持原样, 未追加 ADB GUI 子目录. 三个自动打开开关关闭时, 仍保存有效 1080 x 2424 PNG.
2. 在 A 开始录屏后将偏好改为 B, 当前会话路径仍为 A. 移走 A 后停止保存, UI 准确显示 A 的失败目标、设备源路径和三项恢复动作. 源文件仍有 300723 字节, 等待数分钟未被自动删除或重复保存.
3. 取消另存为、取消放弃均保留原会话. 再次另存为到恢复目录成功, 得到 H.264 / 1080 x 2424 / 133.581522 秒 / 300723 字节的 MP4, ffmpeg 完整解码通过. 保存成功后精确原源路径已不存在.
4. 下一段从 B 开始, 即使录制中恢复公共目录默认值, 自然结束仍保存到 B. 成片 H.264 / 1080 x 2424 / 187.974967 秒 / 315644 字节. 配置仍使用原 180 秒 screenrecord 上限; 不把命令上限写成实际媒体时长. 设备源已清理.
5. 单独恢复目录时三个开关仍关闭; 整组恢复后目录为默认且三个开关均开启. 原生目录选择取消保持原值, 返回后的 Tab 顺序正确, 成功选择返回焦点到选择按钮.
6. 再选择 B, 正常退出并重开原生应用, 新会话仍从 B 开始, 验证目录持久化. 移走 B 后保存失败, 167144 字节源仍保留. 失败等待期间计时保持 00:47, 切页后仍不增长.
7. B 不可用时点击截图并复制, 解码页实际读取到一张剪贴板图片, 未识别到码符合桌面截图内容, 证明复制绕过保存目录. 测试后清空解码页临时结果.
8. 恢复 B, 在失败状态把录屏打开开关设为开启, 点击重试保存. 仍写回原 B 目标, 得到 H.264 / 21.287344 秒 / 167144 字节文件, 完整解码通过, 设备源已清理. QuickTime 实际打开该精确文件, 证明重试使用本次打开选项.
9. 最后一段再次制造目录失效, 点击放弃保存并在系统对话框确认. UI 返回可开始录屏状态, 对应设备源路径已删除, 未出现伪造保存成功.

## 界面与恢复

- Browser 实际打开最终代码, 900x600/1200x800 双主题截图检查无目录行溢出或按钮遮挡. Browser 的原生目录选择明确禁用, 不生成虚假路径, 本轮页面 console error/warn 为空.
- 原生恢复区域长路径完整换行, 重试/另存为/放弃可点击, 新录屏在失败待处理期间禁用.
- 恢复原生捕获偏好为系统默认目录和三个开启开关. 原有亮色主题、关闭的启动更新检查保持原状.
- A/B 测试目录已移回原名, 已关闭本轮 QuickTime 文件窗口. 未修改或删除原设备文件. 应用与模拟器保留可用.
- 最新 debug 包位于 src-tauri/target/debug/bundle/macos/ADB GUI.app, Browser 预览复用 http://127.0.0.1:1420/.

## 产物与范围

| 本机产物, 相对于 /tmp/adb-gui-capture-test.vmHpQY | 结果 |
| --- | --- |
| 截图 目录 A/capture-emulator-5554-14046-1788594351032965000-1.png | 真实 PNG, 1080 x 2424 |
| 恢复 输出/recovered.mp4 | 失败后另存为恢复, 300723 字节 |
| 截图 目录 B/capture-emulator-5554-14046-1788594815982164000-3.mp4 | 自然结束保存, 315644 字节 |
| 截图 目录 B/capture-emulator-5554-19568-1788595733615736000-1.mp4 | 原目录重试保存, 167144 字节 |

- 实机平台为本轮指定的模拟器. 真实手机 ROM、第二台设备切换、Windows/Linux 未做原生验收.
- 离线放弃的残留错误、成功后清理失败、输出冲突/权限失败及迟到并发边界主要由单测和源码检查覆盖, 未逐项进行设备断线和系统权限注入.
- 未在原生保存框中覆盖既有用户文件; 允许覆盖和失败保留由真实临时文件单测验证. 截图自动打开/定位的全部组合未重复第一批实测, 本轮验证关闭仍保存、重置范围、真实复制和录屏打开时点.
- 不承诺跨启动恢复, 不删除或迁移既有文件. 旧版本忽略新设置字段, 用旧版重新保存偏好可能丢弃新增字段.
