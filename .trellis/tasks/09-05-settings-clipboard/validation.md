# 设置与剪贴板验收记录

## 交付状态

- 2026-09-05 用户已通过 "OK开始执行" 授权实现, 两个子任务均已实现.
- 当前 main 分支保留未提交修改. 不运行会自动提交的任务归档和日志脚本.
- 最终 macOS debug 包: `src-tauri/target/debug/bundle/macos/ADB GUI.app`.
- 界面预览: `http://127.0.0.1:1420/`. 原生 ADB 和剪贴板验收使用真实 Tauri 包.

## 自动检查

| 检查 | 实际结果 |
| --- | --- |
| `corepack pnpm test` | 35 个文件, 347 项测试通过 |
| `corepack pnpm build` | TypeScript 和 Vite 构建通过, 保留既有的大 chunk 警告 |
| 60 秒上限 `cargo test` | 119 项测试通过 |
| `cargo fmt --check` | 通过 |
| `cargo clippy --all-targets -- -D warnings` | 通过 |
| `bash -n scripts/build-app-info-dex/build.sh` | 通过 |
| 实际 javac/D8 构建 | 通过, Java 与打包 DEX 已同步 |
| Tauri debug app 构建 | 通过, 最终包包含弹窗首尾焦点循环修复 |
| `git diff --check` | 通过 |

测试命令的完整参数见两个子任务 implement.md. 最后只修改了弹窗键盘处理, 重新验证前端测试和最终 Tauri 构建; Rust 结果来自此前同轮完整门禁, 此后未修改后端.

## 设置界面

- Browser 实际操作 1200 x 800 和 900 x 600, 亮色/暗色下无水平溢出或操作区遮挡.
- 无设备时设置可打开, 各分组可编辑. 侧栏/日志/性能入口共用唯一弹窗.
- 最终修复后实际验证: 首控件 Shift+Tab 到底部关闭, 底部关闭 Tab 到首控件, 焦点均留在弹窗.
- 方向键切换分组, Escape 关闭并返回设置入口通过.
- 运行中指定文件页不会立即跳页, reload 后进入文件页; 恢复上次页面策略也已验证.
- 原生应用退出重开后新偏好保留, 原有亮色主题保留.
- 关闭启动更新检查的零调用和在途失效有单测; 未做原生网络抓包断言.
- 配置损坏/存储失败/分组恢复/运行态隔离有 store 测试; 未对原生 WebKit 存储做故障注入.
- 测试结束已恢复启动更新检查为 true, 截图打开/定位与录屏打开均为 true. 原生主题仍为原有 light.

## 原生截图与录屏

- 在 macOS LaunchServices 启动的 Tauri 应用和 Xiaomi 23113RKC6C (Android 16 / SDK 36, WiFi ADB) 上实际操作.
- 截图的打开图片/定位目录四种组合均保存真实 PNG, Preview/Finder 实际行为与开关一致.
- 关闭录屏自动打开后, 手动停止仍得到有效 H264 MP4: 13.716444 秒, 241514 字节.
- 开启录屏自动打开后, 180 秒自然结束保存有效 H264 MP4: 179.995867 秒, 1440 x 3200, 68391435 字节, QuickTime 实际打开.
- 最终包实际点击截图并复制, 成功写入图片剪贴板. 随后发送到手机明确提示 "无法读取电脑剪贴板中的文本"; 手机内容 SHA-256 前后相同, 未发生目标写入.
- 保存产物位于 `/Users/qi/Pictures/ADB GUI/`, 不删除用户既有文件.

## 剪贴板与共享 DEX

- 原生电脑 -> 手机中文发送成功, 使用独立 DEX get 校验文本一致. 手机 -> 电脑成功后, 在电脑原生输入框实际粘贴并比对独立中文样例.
- Android 前台输入框中的实际粘贴未单独完成; 手机目标值由 API readback 和独立 get 确认.
- DEX 真机往返覆盖中文, LF/CRLF, 表情, 前后空白, 引号, 反引号, shell 片段, 内嵌协议 marker, 控制字符及 256 KiB 文本. 超限拒绝且目标不变.
- 锁屏明确返回 locked, 未更改 appops/输入法/权限或锁屏配置.
- 原应用信息入口与剪贴板并发成功, 原入口返回 227 个应用. 共用部署服务不占应用查询的长锁.
- 设备 8 秒超时实测退出 137, 8094 ms, 无残留剪贴板 helper.
- 协议错误, UTF-8 长度/转义膨胀, 非零退出, 有界管道, 部署失败清理和别名共享锁有 Rust 测试.
- A -> B -> A, 断线/授权丢失/传输变化, 迟到 get/readHost/finally 与禁止写入重试有控制器测试.
- 只在用户点击时读源内容; 正文经 stdin JSON 传输, 不进入命令参数, 持久化设置或应用诊断日志.

## 未覆盖范围

- Android 9 等旧版本及其他 ROM, Windows/Linux 原生剪贴板未实测. 本轮只有当前 Android 16 真机完成联合验收.
- 没有进行真实设备切换/断线故障注入, 工作资料/非主用户拒绝, 旧 DEX 替换失败及 USB/WiFi 别名并发部署的真机测试.
- 性能后台采集/手动暂停/断线的完整原生组合未实测, 现有控制器和 store 回归测试通过.
- 应用信息与剪贴板并发已实测; 图标查询完整 90 秒压力场景和最终包图标逐项比对未执行.
- 上述范围不记为真机通过, 后续兼容性验收从这些项目继续.
