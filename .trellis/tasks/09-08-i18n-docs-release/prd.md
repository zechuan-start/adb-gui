# 双语文档与截图

父任务: [界面中英双语与语言设置](../09-08-i18n-bilingual/prd.md)

依赖: 子任务 2, 3, 4 全部完成. 界面还有中文残留时出图无意义.

## 交付边界

让英文 README 配英文界面截图, 中文 README 配中文界面截图, 并补齐语言相关的静态元数据. 当前 `README.md` (英文) 引用的七张 `docs/images/*.png` 全部是中文界面.

## 范围内

- `scripts/screenshots/capture.mjs` 支持按语言出图, 输出到区分语言的路径.
- `scripts/screenshots/mock-tauri.js` 的模拟数据: 界面文案随语言变化, 但模拟的设备数据 (应用名, 日志正文, 文件名) 保持原样, 与真实产品行为一致.
- 重新生成两套截图.
- `README.md` 引用英文截图; `README.zh-CN.md` 引用中文截图.
- 两份 README 补语言设置的说明段落.
- `index.html` 的 `lang` 属性处理说明 (运行时由前端写入, 静态值保留 `en`).

## 范围外

- 不改 `productName` 与窗口标题, 品牌名保持 `ADB GUI`.
- 不重写 README 的其他内容.
- 不做第三语言.

## 待确认

- 截图输出路径方案. 推荐 `docs/images/en/*.png` 与 `docs/images/zh-CN/*.png` 两个目录, 两份 README 各引各的; 代价是所有现有图片路径都要改. 备选是英文图沿用现路径, 中文图放 `docs/images/zh-CN/`, 改动更小但目录含义不对称.

## 验收标准

- [ ] `pnpm screenshots` 能一次生成两套图, 或通过参数分别生成, 命令写进 README 的贡献说明.
- [ ] 英文 README 的七张图全部为英文界面, 中文 README 的七张图全部为中文界面.
- [ ] 截图里的模拟设备数据 (应用名, 日志正文) 两套图一致, 只有界面文案不同.
- [ ] 两份 README 都说明了语言设置的位置与"跟随系统, 非中文用英文"的默认行为.
- [ ] 图片没有出现英文长文案被截断的构图.
- [ ] 重跑脚本产出稳定 (同一份代码两次生成的图内容一致).
