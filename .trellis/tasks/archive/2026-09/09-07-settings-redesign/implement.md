# 实施与收口: 设置面板整体重构

2026-09-07 完成主体实现, 2026-09-08 完成审查修复, 视觉调整和原生验收. 采用单任务串行实施, 后续改动沿用本任务. 分日期验证记录见 `validation.md`.

## 实现清单

- [x] 注册表从 groups 扁平为 rows, 删除搜索与旧行过滤原语, 保留分组顺序, 存储归属, 重置计划和迁移链.
- [x] 新增共享 Switch, SegmentedControl, ChipGroup, SettingSwitchRow 与 SettingsSectionBlock, 六组统一行布局, 标签和已修改标记.
- [x] 文件, 应用和生码快捷控件继续使用与设置面板相同的 store action, 不增加偏好字段或修改默认值.
- [x] 弹窗改为六组单页滚动, 左侧锚点索引, scrollspy 和分组就地重置; 删除搜索, 二级小标题和 footer.
- [x] 错误横幅与恢复操作固定在滚动区外, 索引 Tab 进入目标分组控件, 禁用范围保持按存储归属划分.
- [x] 下拉菜单按视口与滚动祖先交集避让边界, 支持上下展开与可用高度约束; 移除原生 WebKit 绘制异常的缩放入场动画.
- [x] macOS 原生菜单增加 "设置…" 与 `Cmd + ,`, 通过事件打开现有弹窗; Windows 不注册该入口.
- [x] 设置内容区添加淡网格, 亮色与暗色分别采用主界面线色的 60% 与 50%, 控件及菜单保持实色.
- [x] 首次打开聚焦无边框标题; Tab / Shift+Tab 进入首尾控件并保留可见焦点, 分组导航不重置标题焦点.
- [x] 同步 README 快捷键说明和前端组件, 设置契约.

## 验证清单

- [x] `pnpm test`: 42 个测试文件, 410 项通过, 2026-09-08 提交前复跑通过.
- [x] `pnpm build`: TypeScript 与 Vite 构建通过; 最终焦点修复后再次构建通过.
- [x] `cargo fmt --manifest-path src-tauri/Cargo.toml --check` 与 Clippy 通过; 125 项 Rust 测试在 60 秒硬超时内通过. 后续改动仅涉及前端和文档, 复用这些结果.
- [x] `git diff --check` 通过; 旧设置原语和原生 checkbox 搜索无残留.
- [x] 浏览器验证双尺寸双主题, 六分组可达, 无横向溢出, 各工作区入口定位与共享偏好同步, 重置, 错误禁用范围和键盘导航.
- [x] 原生 macOS 验证快捷键, 错误与恢复入口, 900x600 / 1200x800 菜单完整显示, 自定义分隔符, 分组重置及全局重置确认.
- [x] 最终原生版本验证淡网格, 标题初始焦点无边框, 按 Tab 后仍显示控件焦点框, Escape 后可再次通过 `Cmd + ,` 打开.
- [x] 完成测试后恢复测试修改的偏好; 最终 release App 已安装到 `/Applications/ADB GUI.app`, 签名校验通过, 安装文件哈希与构建产物一致. 替换下来的旧 App 已移入垃圾桶.

## 收口约定

- 用户已于 2026-09-08 授权整理, 结束本任务, 提交并推送当前 `main` 分支.
- 归档本任务, 同时提交此前已获授权并完成的 `09-05-settings-panel-refactor` 归档; 保留其他任务状态.
- 将代码与契约提交和任务归档记录分开, 核对暂存范围后提交; 在任务元数据与工作日志记录实现提交.
- 本次不发布版本, 不生成更新发布资产, 不宣称完成 Windows 原生验收.
